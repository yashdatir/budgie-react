import { PAGE2_INITIAL_DATA } from "../../../Utils/reduxConstants";

export const page2Reducer = (state = PAGE2_INITIAL_DATA, action) => {
    switch(action.type){
        default:{
            return state
        }
    }
}