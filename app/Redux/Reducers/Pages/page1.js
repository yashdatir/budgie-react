import { PAGE1_INITIAL_DATA } from "../../../Utils/reduxConstants";

export const page1Reducer = (state = PAGE1_INITIAL_DATA, action) => {
    switch(action.type){
        default:{
            return state
        }
    }
}